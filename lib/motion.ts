"use client";

import { motion } from "framer-motion";
import * as animations from "@/lib/motionAnimations";

export const animation = animations;
export const section = motion.section;
export const div = motion.div;
